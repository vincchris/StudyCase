/*
Challenge 03: Pass or Fail

Determine whether a student passes or fails based on the score.

Rules:
- Score >= 75 → Pass
- Otherwise → Fail

Example:
Score = 80

Output:
Pass
*/

let score = 80

if (score >= 75) {
  console.log("Pass")
}

else {
  console.log("Fail")
}